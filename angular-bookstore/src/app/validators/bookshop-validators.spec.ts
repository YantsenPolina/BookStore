import { BookshopValidators } from './bookshop-validators';

describe('BookshopValidators', () => {
  it('should create an instance', () => {
    expect(new BookshopValidators()).toBeTruthy();
  });
});
